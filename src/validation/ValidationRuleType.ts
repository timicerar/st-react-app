export enum ValidationRuleType {
    MatchRule = "match",
    PatternRule = "pattern",
    RequireRule = "require",
    MinMaxRule = "min-max",
    MinRule = "min",
    SumRule = "sum",
    LengthRule = "length",
    DateRule = "date-rule",
}
